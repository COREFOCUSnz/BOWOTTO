<?xml version="1.0" encoding="utf-8"?>
<!--
    Belt and braces on top of android:allowBackup="false": even if a future
    build flips that flag, the vault directory and the key material must never
    be swept into a cloud or device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
