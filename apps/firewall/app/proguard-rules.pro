# The vault format is hand-rolled binary I/O with no reflection, so nothing
# needs keeping beyond the defaults. Compose ships its own rules.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
